---
import Layout from '../layouts/Layout.astro';
import PublicationCard from '../components/PublicationCard.astro';
import publications from '../data/publications.json';

const featured = publications.find((p) => p.status === 'available');
---

<Layout title="Eric Gustafson — Author of Mexico Viking">
  <section class="section hero">
    <div class="container hero__inner">
      <div class="hero__text">
        <h1>Eric Gustafson</h1>
        <p class="hero__lead">
          Author of <strong>Mexico Viking</strong>, a novel about Scandinavian and
          Mexican roots, adventure, and the fight for environmental conservation.
        </p>
        <div class="hero__actions">
          <a class="btn btn-primary" href="/publications">View Publications</a>
          <a class="btn btn-secondary" href="/bio">About the Author</a>
        </div>
      </div>
      {
        featured?.image && (
          <div class="hero__cover">
            <img src={featured.image} alt={`Cover of ${featured.title}`} />
          </div>
        )
      }
    </div>
  </section>

  {
    featured && (
      <section class="section section--muted">
        <div class="container">
          <div class="section-heading section-heading--underline">
            <h2>About the Book</h2>
          </div>
          <div class="book-highlight">
            <p>{featured.description}</p>
            <a class="btn btn-secondary" href="/publications">
              Read More
            </a>
          </div>
        </div>
      </section>
    )
  }

  {
    featured && (
      <section class="section">
        <div class="container">
          <div class="section-heading">
            <h2>Latest Release</h2>
          </div>
          <div class="latest-book">
            <PublicationCard publication={featured} />
          </div>
        </div>
      </section>
    )
  }
</Layout>

<style>
  .hero {
    padding-top: var(--space-3xl);
  }

  .hero__inner {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-3xl);
    align-items: center;
  }

  @media (min-width: 768px) {
    .hero__inner {
      grid-template-columns: 3fr 2fr;
      gap: var(--space-4xl);
    }
  }

  .hero__lead {
    font-size: 1.15rem;
    color: var(--color-text-muted);
    max-width: 46ch;
  }

  .hero__actions {
    display: flex;
    gap: var(--space-md);
    flex-wrap: wrap;
    margin-top: var(--space-lg);
  }

  .hero__cover {
    background: var(--color-bg-muted);
    border-radius: var(--radius);
    overflow: hidden;
    aspect-ratio: 2 / 3;
    max-width: 320px;
    margin: 0 auto;
  }

  .hero__cover img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .book-highlight {
    max-width: 70ch;
  }

  .latest-book {
    max-width: 360px;
  }
</style>
